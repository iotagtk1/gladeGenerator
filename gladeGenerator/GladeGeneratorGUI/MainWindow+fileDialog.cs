using System;
using Gtk;
using UI = Gtk.Builder.ObjectAttribute;

namespace GladeGeneratorGUI
{
    partial class MainWindow
    {
    
    
		private void on_cancelBtn_clicked(object sender , EventArgs e)
		{



			/*ListStore_fileDialog.Foreach (delegate (ITreeModel model, TreePath path, TreeIter iter)  {
			
			    type_fileDialog modelObj = model.GetValue(iter, 0) as type_fileDialog;
			    
			    return false;
			});*/


		}
		private void on_okBtn_clicked(object sender , EventArgs e){
			/*ListStore_fileDialog.Foreach (delegate (ITreeModel model, TreePath path, TreeIter iter)  {
			
			    type_fileDialog modelObj = model.GetValue(iter, 0) as type_fileDialog;
			    
			    return false;
			});*/

			
		}}
}
