/*
TreeIter iter;
if (listStore_{num}.GetIterFromString(out iter, args.Path))
{
    //({Model}){ListStore}
    {Model} modelObj = ({Model}){ListStore}.GetValue(iter, 0);
}
if (listStore_{num}.GetIter(out iter, e.Path))
{
    //({Model}){ListStore}
    {Model} modelObj = ({Model}){ListStore}.GetValue(iter, 0);
}
*/